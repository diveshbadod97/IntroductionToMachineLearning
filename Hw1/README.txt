q1.py and q2.py don't need any addotional requirements to run them except for some libraries like numpy, matplotlib and pandas

q2.py though it wasn't required for the model to predict values there are some lines commented out which let's the model predict the values
for the given dataset to check the accuracy you can just remove the comments